---
phase: quick
plan: 260413-1kn
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/SidebarShell.tsx
  - src/components/StepChecklist.tsx
autonomous: true
requirements:
  - compact-header-redesign
  - remove-step-border
  - copy-button-contrast
must_haves:
  truths:
    - "Header row is a single compact line showing cleaned task label, + button, and gear icon"
    - "cleanedLabel capitalizes first word, trims to ~40 chars with ellipsis, strips trailing ? or ."
    - "+ button triggers handleNewTask and shows 'New task' tooltip on hover"
    - "Gear icon sits in the same single row as the label and + button"
    - "Header height is visually compact (target ~36-40px), not a tall block"
    - "Current step in StepChecklist has no blue left border — highlight background only"
    - "Copy button (Clipboard icon) is clearly visible without hover using var(--color-text-primary)"
  artifacts:
    - path: src/components/SidebarShell.tsx
      provides: Compact single-row header replacing two-stacked-element header
    - path: src/components/StepChecklist.tsx
      provides: Removed left-border accent, stronger copy button color
  key_links:
    - from: header row
      to: handleNewTask
      via: onClick on + button
    - from: header row
      to: setShowSettings
      via: onClick on gear icon (unchanged)
---

<objective>
Three focused UI polish changes: compact single-row header, remove blue left-border from
current step, and stronger copy button contrast.

Purpose: Reduce visual clutter and improve clarity of key interactive elements.
Output: Updated SidebarShell.tsx header section and StepChecklist.tsx copy button + step border.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compact single-row header in SidebarShell</name>
  <files>src/components/SidebarShell.tsx</files>
  <action>
    Replace the two-element stacked header block (lines ~479-558) with a single compact row.

    **Add a `Plus` import** from `lucide-solid` alongside the existing `Settings` import at the top.

    **Add a `cleanLabel` helper function** directly inside the component (before the return):

    ```ts
    const cleanLabel = (raw: string): string => {
      let s = raw.trim().replace(/[?.]+$/, "");   // strip trailing ? or .
      s = s.charAt(0).toUpperCase() + s.slice(1); // capitalize first word
      return s.length > 40 ? s.slice(0, 40) + "\u2026" : s;
    };
    ```

    **Replace the task header `<Show when={lastIntent().length > 0}>` block** (currently lines ~513-558) with:

    ```tsx
    <Show when={lastIntent().length > 0}>
      <div
        aria-live="polite"
        style={{
          background: "var(--color-surface-secondary)",
          "border-bottom": "1px solid var(--color-border)",
          padding: "0 var(--space-md)",
          "flex-shrink": "0",
          display: "flex",
          "align-items": "center",
          gap: "var(--space-xs)",
          height: "38px",
          "min-height": "38px",
        }}
      >
        {/* Cleaned task label — fills remaining space, truncates */}
        <span
          style={{
            "font-size": "var(--font-size-label)",
            "font-weight": "500",
            "line-height": "1",
            color: "var(--color-text-secondary)",
            overflow: "hidden",
            "text-overflow": "ellipsis",
            "white-space": "nowrap",
            flex: "1",
            "min-width": "0",
          }}
        >
          Working on: {cleanLabel(lastIntent())}
        </span>

        {/* + button — new task */}
        <button
          onClick={handleNewTask}
          aria-label="New task"
          title="New task"
          style={{
            border: "none",
            background: "transparent",
            color: "var(--color-text-secondary)",
            cursor: "pointer",
            display: "flex",
            "align-items": "center",
            "justify-content": "center",
            padding: "0",
            width: "28px",
            height: "28px",
            "flex-shrink": "0",
            "border-radius": "var(--radius-sm)",
          }}
          onMouseEnter={(e) => {
            (e.currentTarget as HTMLButtonElement).style.color = "var(--color-text-primary)";
          }}
          onMouseLeave={(e) => {
            (e.currentTarget as HTMLButtonElement).style.color = "var(--color-text-secondary)";
          }}
        >
          <Plus size={14} />
        </button>

        {/* Gear icon — settings, same row */}
        <button
          onClick={() => setShowSettings(true)}
          aria-label="Open skill profile and settings"
          title="Skill profile"
          style={{
            border: "none",
            background: "transparent",
            color: "var(--color-text-secondary)",
            cursor: "pointer",
            display: "flex",
            "align-items": "center",
            "justify-content": "center",
            padding: "0",
            width: "28px",
            height: "28px",
            "flex-shrink": "0",
            "border-radius": "var(--radius-sm)",
          }}
          onMouseEnter={(e) => {
            (e.currentTarget as HTMLButtonElement).style.color = "var(--color-text-primary)";
          }}
          onMouseLeave={(e) => {
            (e.currentTarget as HTMLButtonElement).style.color = "var(--color-text-secondary)";
          }}
        >
          <Settings size={14} />
        </button>
      </div>
    </Show>
    ```

    **Also remove the standalone gear-only header div** (lines ~479-508 — the existing `justify-content: flex-end` header) because the gear icon is now embedded in the task header row. However this header is always visible even when no task is active. Instead keep it only when `lastIntent()` is empty, so gear is still accessible from the empty state:

    Change the standalone gear header to:
    ```tsx
    <Show when={lastIntent().length === 0}>
      <div style={{
        display: "flex",
        "justify-content": "flex-end",
        "align-items": "center",
        padding: "var(--space-xs) var(--space-md)",
        "border-bottom": "1px solid var(--color-border)",
        "flex-shrink": "0",
      }}>
        <button
          onClick={() => setShowSettings(true)}
          aria-label="Open skill profile and settings"
          title="Skill profile"
          style={{
            border: "none",
            background: "transparent",
            color: "var(--color-text-secondary)",
            cursor: "pointer",
            "min-height": "44px",
            "min-width": "44px",
            display: "flex",
            "align-items": "center",
            "justify-content": "center",
            padding: "0",
          }}
        >
          <Settings size={14} />
        </button>
      </div>
    </Show>
    ```

    This ensures the gear is always reachable: in empty state via the standalone header, in active task state via the compact row.
  </action>
  <verify>
    Run `npm run check` (or `npx tsc --noEmit`) from the project root — should pass with no TS errors.
    Visually: with a task active, the header should be a single ~38px row with truncated label, + icon, and gear icon aligned right.
  </verify>
  <done>
    - Single compact header row visible when lastIntent is set
    - Label shows "Working on: {cleanedLabel}" — capitalized, max 40 chars, no trailing ?/.
    - + button calls handleNewTask and shows tooltip "New task"
    - Gear icon present in same row on the right
    - Standalone gear header shown only when no task is active (empty state)
    - No TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove left-border from current step and strengthen copy button</name>
  <files>src/components/StepChecklist.tsx</files>
  <action>
    Two targeted changes in `StepChecklist.tsx`.

    **Change 1 — Remove blue left border from current step.**

    In the `<button>` element's inline style (around line 78-96), the `border-left` property currently shows `3px solid var(--color-accent)` for current step and `3px solid transparent` for others.

    Replace both `"border-left"` values so neither shows a colored bar:

    ```diff
    - "border-left": isCurrent()
    -   ? "3px solid var(--color-accent)"
    -   : "3px solid transparent",
    + "border-left": "3px solid transparent",
    ```

    This keeps column width stable (no layout jump) while removing the accent bar. The background highlight (`var(--color-surface-secondary)`) still identifies the current step.

    **Change 2 — Stronger copy button color.**

    In the copy button's inline style (around line 166), change the base color from `var(--color-text-secondary)` to `var(--color-text-primary)`:

    ```diff
    - color: "var(--color-text-secondary)",
    + color: "var(--color-text-primary)",
    ```

    Also update the `onMouseLeave` handler to restore to `var(--color-text-primary)` instead of `var(--color-text-secondary)`:

    ```diff
      onMouseLeave={(e) => {
        if (copiedIndex() !== index()) {
          (e.currentTarget as HTMLButtonElement).style.color =
    -       "var(--color-text-secondary)";
    +       "var(--color-text-primary)";
        }
      }}
    ```

    The `onMouseEnter` can remain `var(--color-text-primary)` (already correct) or be removed since it now matches the base — either is fine.
  </action>
  <verify>
    Run `npm run check` (or `npx tsc --noEmit`) — no errors.
    Visually: current step row shows only the background highlight, no left blue bar. Clipboard icon is clearly visible at rest without hovering.
  </verify>
  <done>
    - Current step: no blue left border; background highlight still present
    - `border-left: 3px solid transparent` retained for layout stability
    - Copy button Clipboard icon uses `var(--color-text-primary)` at rest
    - `onMouseLeave` restores to `var(--color-text-primary)` (not secondary)
    - No TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lastIntent signal → DOM | User-supplied text rendered into header label |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1kn-01 | Tampering | cleanLabel renders lastIntent into DOM | accept | SolidJS text interpolation (`{cleanLabel(lastIntent())}`) is XSS-safe — no innerHTML or dangerouslySetInnerHTML used. String is truncated before render. |
</threat_model>

<verification>
- `npm run check` passes with no TypeScript errors
- Header with active task: single ~38px row, label + `+` + gear on same line
- Header without active task: standalone gear header visible
- cleanLabel: "how do i fix gitt conflict in the terminal" → "How do i fix gitt conflict in the ter…" (40 chars)
- cleanLabel strips trailing "?" and "."
- StepChecklist current step: background highlight only, no blue left bar
- Copy button Clipboard icon visible without hover
</verification>

<success_criteria>
1. Header is a compact single row when a task is active — no stacked two-element layout
2. Cleaned label is readable as system state text (secondary color, slightly bold)
3. + button and gear icon are right-aligned in the same row
4. No blue vertical accent bar on current step in StepChecklist
5. Copy button icon visible at rest (primary color)
6. No regressions — TypeScript compiles clean
</success_criteria>

<output>
After completion, create `.planning/quick/260413-1kn-compact-header-redesign-cleaned-task-lab/260413-1kn-SUMMARY.md`
</output>
