---
phase: quick
plan: 260413-09m
subsystem: frontend-ui
tags: [copy-button, step-checklist, session-history, collapsible, ux]
dependency_graph:
  requires: []
  provides: [inline-backtick-copy-button, collapsible-session-history]
  affects: [StepChecklist, SidebarShell, SessionFeed]
tech_stack:
  added: []
  patterns: [extractInlineCommand, expandedHistoryItems Set signal, collapsible For loop]
key_files:
  created: []
  modified:
    - src/components/StepChecklist.tsx
    - src/components/SidebarShell.tsx
decisions:
  - "extractInlineCommand() lives in StepChecklist.tsx (presentation layer only) — parseSteps.ts untouched"
  - "expandedHistoryItems uses Set<number> of indices; reset to empty Set on each new submission (indices shift after slice)"
  - "SessionFeed receives sessionHistory=[] — history rendering moved to inline For loop in SidebarShell"
metrics:
  duration: "~10 minutes"
  completed: "2026-04-13T05:14:44Z"
  tasks_completed: 2
  files_modified: 2
---

# Quick Task 260413-09m: Fix Copy Buttons for Inline Backtick Commands + Collapsible History Summary

**One-liner:** Restore copy buttons for prose-prefixed backtick steps via `extractInlineCommand()` and collapse prior session history to one-line toggle rows.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Fix copy button — extract inline backtick command | bb243b0 | src/components/StepChecklist.tsx |
| 2 | Collapsible session history in SidebarShell | a6fcebe | src/components/SidebarShell.tsx |

## What Was Built

### Task 1 — StepChecklist.tsx

Added `extractInlineCommand(label: string): string | null` which:
- Matches the first backtick-wrapped token via `` /`([^`]+)`/ ``
- Returns the token only if it matches `COMMAND_PATTERN` (is a recognizable CLI command)
- Otherwise returns `null`

Updated `showCopyButton` to `inlineCmd() !== null || isCommandLine(step.label)` — covers both new prose-prefix style and legacy direct-command style.

Updated copy button `onClick` to `copyToClipboard(inlineCmd() ?? step.label)` — copies the command token when present, falls back to full label for legacy lines.

Examples that now work:
- `'Run \`git branch -a\` to see all available branches'` → copy button shown, copies `git branch -a`
- `'Switch to local branch with \`git checkout branch-name\`'` → copy button shown, copies `git checkout branch-name`
- `'Open the File menu in the top bar'` → no copy button
- `'git status'` (legacy) → copy button shown, copies `git status`

### Task 2 — SidebarShell.tsx

Added `expandedHistoryItems` signal (`Set<number>`) for per-item collapse state.

Replaced the `sessionHistory` prop on `<SessionFeed>` with an inline `<For>` loop that renders collapsible rows:
- Collapsed state: one-line truncated intent summary + chevron (`▶`/`▼`)
- Expanded state: full guidance text rendered below the header row
- Toggle via button click; uses `setExpandedHistoryItems` with a new Set copy

`SessionFeed` now receives `sessionHistory={[]}` — it only handles the live streaming exchange.

Reset `expandedHistoryItems` to `new Set<number>()` in:
- `submitIntent` — after pushing currentExchange to history (indices shift)
- `handleNewTask` — full session reset

`For` imported from `solid-js` (was not previously imported).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] TypeScript type inference on `new Set()` calls**
- **Found during:** Task 2 TypeScript check
- **Issue:** `new Set()` inferred as `Set<unknown>` by TypeScript, not assignable to `Set<number>` signal setter
- **Fix:** Changed all three `new Set()` calls to `new Set<number>()` (signal init + two reset sites)
- **Files modified:** src/components/SidebarShell.tsx
- **Commit:** a6fcebe

## Known Stubs

None.

## Threat Flags

None. The `extractInlineCommand` function extracts AI-generated text passed to `navigator.clipboard` — threat T-09m-01 was assessed as `accept` in the plan's threat model (clipboard content is user-visible before use; no code execution path).

## Self-Check: PASSED

- src/components/StepChecklist.tsx — modified, committed bb243b0
- src/components/SidebarShell.tsx — modified, committed a6fcebe
- No new TypeScript errors introduced (pre-existing unused variable warnings in SidebarShell.tsx remain, unrelated to this task)
