---
phase: quick
plan: 260413-1kk
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai.ts
autonomous: true
requirements:
  - QUICK-1kk
must_haves:
  truths:
    - "Steps with commands use the 'Run: `cmd`' pattern — no trailing explanation on the same line"
    - "No orientation or navigation steps added unless user explicitly indicated they're elsewhere"
    - "Steps are minimal — no 'make sure you're in X' or redundant context lines"
    - "First line of guidance is still '1.' with no preamble"
    - "Clarifying question behavior unchanged"
  artifacts:
    - path: "src/lib/ai.ts"
      provides: "Updated SYSTEM_PROMPT with command-first rules"
      contains: "Run:"
  key_links:
    - from: "SYSTEM_PROMPT"
      to: "Claude API"
      via: "system field in streamGuidance POST body"
      pattern: "system: systemPrompt"
---

<objective>
Rewrite SYSTEM_PROMPT in src/lib/ai.ts to enforce command-first step formatting.

Purpose: Current prompt produces verbose steps with trailing explanations ("Run `git status` to see which files..."). The new format uses "Run: `cmd`" — action verb + colon + command, nothing after. Steps assume the user is already in context; no orientation steps.

Output: Updated SYSTEM_PROMPT constant. No other changes to the file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/lib/ai.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SYSTEM_PROMPT with command-first rules</name>
  <files>src/lib/ai.ts</files>
  <action>
Replace the SYSTEM_PROMPT string (lines 3–19 of src/lib/ai.ts) with the revised version below. Do NOT touch anything outside the SYSTEM_PROMPT constant — leave TIER_SUFFIX, streamGuidance, imports, and all other code unchanged.

New SYSTEM_PROMPT:

```
export const SYSTEM_PROMPT = `You are a task execution assistant.

STRICT RULES:
- When providing steps: Start with "1." on the FIRST LINE, no intro sentence.
- Do NOT describe the screen.
- Do NOT explain context.
- ONLY output numbered steps when providing guidance.
- Assume the user is already in the right place. Do NOT add navigation or orientation steps unless they are genuinely required to reach the target location.

Each step must:
- Begin with its number followed by a period (1., 2., etc.)
- Contain exactly ONE actionable instruction
- For steps that include a command, use the format: Verb: \`command\`
  Examples: "Run: \`git status\`", "Open: \`code filename.ts\`", "Install: \`npm install\`"
- No trailing explanation after the command on the same line. The verb names the action; the command is the full instruction.
- Reference visible UI elements by label, color, and position when needed: "Click the blue 'New' button in the top-left toolbar"
- Put terminal commands or code INLINE using backticks — do NOT use markdown code blocks (\`\`\`) inside numbered steps

If the user's intent is vague or unclear, respond with a single plain-text question (NOT a numbered list). Example: "Which file are you trying to open?"
If a step requires waiting (loading, processing), say so in that step.`;
```

The key changes from the old prompt:
1. Added rule: "Assume the user is already in the right place..."
2. Replaced the old backtick rule with the new command-first format: "Verb: `command`" with no trailing explanation after the command
3. Kept all other rules (start with "1.", one action per step, UI element references, no code blocks, clarifying question behavior)
  </action>
  <verify>
    <automated>grep -n "Run:" src/lib/ai.ts && grep -n "Assume the user" src/lib/ai.ts</automated>
  </verify>
  <done>
    - SYSTEM_PROMPT contains the "Verb: `command`" format rule
    - SYSTEM_PROMPT contains the "Assume the user is already in the right place" rule
    - No other code in src/lib/ai.ts is modified (TIER_SUFFIX, streamGuidance unchanged)
    - `grep -n "TIER_SUFFIX" src/lib/ai.ts` still shows the TIER_SUFFIX constant intact
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SYSTEM_PROMPT → Claude API | Prompt text sent as the `system` field — content shapes model behavior but crosses no additional trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1kk-01 | Tampering | SYSTEM_PROMPT text | accept | Prompt is static source code; no runtime injection path. Rewrite is controlled via source edit only. |
</threat_model>

<verification>
1. Open src/lib/ai.ts and confirm SYSTEM_PROMPT starts with the same opening line
2. Confirm "1." rule is still on the first line rule
3. Confirm the new command-first rule is present: "Verb: `command`"
4. Confirm the no-navigation-assumption rule is present
5. Confirm TIER_SUFFIX constant is unchanged below SYSTEM_PROMPT
6. Run the app and test a command-heavy intent (e.g. "resolve a git merge conflict") — steps should be "Run: `git status`" style, not "Run `git status` to see..."
</verification>

<success_criteria>
- SYSTEM_PROMPT updated with command-first formatting rules
- Guidance output uses "Run: `cmd`" pattern with no trailing prose on command lines
- No orientation/navigation steps injected when not needed
- All other SYSTEM_PROMPT rules (step numbering, clarifying questions, no code blocks) preserved
- TIER_SUFFIX and streamGuidance function untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260413-1kk-update-system-prompt-to-command-first-fo/260413-1kk-SUMMARY.md`
</output>
