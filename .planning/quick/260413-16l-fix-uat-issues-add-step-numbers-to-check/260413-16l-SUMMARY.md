---
phase: quick
plan: 260413-16l
subsystem: frontend-ui
tags: [step-checklist, uat-fix, highlight-state, step-numbers]
dependency_graph:
  requires: []
  provides: [step-number-labels, explicit-highlight-tracking]
  affects: [StepChecklist, SidebarShell]
tech_stack:
  added: []
  patterns: [prop-driven-state, explicit-signal-over-derived]
key_files:
  created: []
  modified:
    - src/components/StepChecklist.tsx
    - src/components/SidebarShell.tsx
decisions:
  - "currentStepIndex moved from derived (findIndex first-incomplete) to explicit signal in SidebarShell — follows tapped step not completion order"
  - "isCurrent keeps && !step.completed guard so a completed-but-last-tapped step shows strikethrough not highlight"
metrics:
  duration: "10m"
  completed: "2026-04-13"
  tasks_completed: 2
  files_changed: 2
---

# Quick 260413-16l: Fix UAT Issues — Add Step Numbers to Checklist Summary

**One-liner:** Prop-driven currentStepIndex signal in SidebarShell replaces derived first-incomplete logic, and step number spans (1, 2, 3...) rendered before each checkbox icon.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Add currentStepIndex signal to SidebarShell, thread as prop to StepChecklist, update onToggle to set it | 374b412 |
| 2 | Render step number label before each checkbox in StepChecklist | 374b412 |

## What Was Built

**Task 1 — Explicit highlight state:**
- Added `const [currentStepIndex, setCurrentStepIndex] = createSignal<number>(0)` in SidebarShell after the `steps` signal
- Reset `setCurrentStepIndex(0)` in `submitIntent` (after `setSteps([])`) and `handleNewTask` (after `setSteps([])`)
- Updated `onToggle` in the StepChecklist JSX to call `setCurrentStepIndex(index)` before mutating steps
- Passed `currentStepIndex={currentStepIndex()}` as a new prop to StepChecklist
- Added `currentStepIndex: number` to `StepChecklistProps` interface
- Removed the derived `const currentStepIndex = () => props.steps.findIndex(...)` computation
- Updated `isCurrent` to `() => index() === props.currentStepIndex && !step.completed`

**Task 2 — Step number labels:**
- Added a `<span>` rendering `{index() + 1}` as the first child of each step `<button>`, before the checkbox Show block
- Styled with `color: var(--color-text-secondary)`, `min-width: 16px`, `text-align: right`, `flex-shrink: 0` — aligns numbers and keeps them muted

## Verification

- `npx tsc --noEmit` produces zero errors in StepChecklist.tsx or SidebarShell.tsx
- 7 pre-existing TS6133 errors on unused unlisten variables (lines 90-96 of SidebarShell.tsx) are out of scope — not introduced by this task

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Threat Flags

None — all changes are local UI state with no new network endpoints or trust boundaries.

## Self-Check: PASSED

- `src/components/StepChecklist.tsx` — modified, verified present
- `src/components/SidebarShell.tsx` — modified, verified present
- Commit `374b412` — verified in git log
