---
phase: 09-state-machine-conversation-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai.ts
autonomous: true
requirements:
  - SESS-01

must_haves:
  truths:
    - "streamGuidance accepts an optional conversationHistory parameter"
    - "Prior turns are prepended to the messages array as text-only before the current user turn"
    - "When conversationHistory is empty or absent, streamGuidance behavior is identical to before"
  artifacts:
    - path: "src/lib/ai.ts"
      provides: "Extended StreamGuidanceOptions interface + updated messages construction"
      contains: "conversationHistory"
  key_links:
    - from: "src/lib/ai.ts StreamGuidanceOptions.conversationHistory"
      to: "/chat endpoint messages array"
      via: "JSON.stringify body.messages"
      pattern: "conversationHistory"
---

<objective>
Extend `streamGuidance` in `src/lib/ai.ts` to accept an optional `conversationHistory` parameter and prepend prior turns (text-only) to the Claude messages array before the current user turn.

Purpose: SESS-01 — follow-up queries must be resolved with structured task context. The Worker `/chat` endpoint already accepts and proxies an arbitrary messages array. This task adds the frontend-side wiring so prior turns reach Claude.

Output: Modified `src/lib/ai.ts` with backward-compatible API extension.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-state-machine-conversation-continuity/09-CONTEXT.md
@.planning/phases/09-state-machine-conversation-continuity/09-RESEARCH.md
</context>

<interfaces>
<!-- Existing interface in src/lib/ai.ts — executor reads the full file before editing -->
export interface StreamGuidanceOptions {
  token: string;
  screenshot: string | null;
  userIntent: string;
  onToken: (text: string) => void;
  onError: (error: string) => void;
  onDone: () => void;
  signal?: AbortSignal;
  tier?: number;
  memoryContext?: string;
  taskLabel?: string;
  appContext?: string;
}

// Current messages construction (single-turn):
body: JSON.stringify({
  model: "claude-sonnet-4-20250514",
  messages: [{ role: "user", content: userContent }],
  system: systemPrompt,
  max_tokens: 4096,
})
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend StreamGuidanceOptions with conversationHistory and update messages construction</name>
  <files>src/lib/ai.ts</files>

  <read_first>
    - src/lib/ai.ts — read in full before editing; understand current StreamGuidanceOptions interface (line 29), streamGuidance function signature (line 45), userContent construction (lines 57-69), and the fetch body (lines 73-86)
    - .planning/phases/09-state-machine-conversation-continuity/09-CONTEXT.md — D-08 (messages format), D-09 (3-turn cap)
    - .planning/phases/09-state-machine-conversation-continuity/09-RESEARCH.md — Pattern 3 (signature extension), Pattern 2 (messages array construction)
  </read_first>

  <action>
  Add an optional field to the `StreamGuidanceOptions` interface (after `appContext`):

  ```typescript
  // Phase 9: SESS-01 — prior turns for multi-turn conversation (D-08)
  conversationHistory?: Array<{ role: "user" | "assistant"; content: string }>;
  ```

  In the `streamGuidance` function body, destructure the new field:
  ```typescript
  const { tier = 1, memoryContext, appContext, conversationHistory } = opts;
  ```

  Update the messages array in the fetch body from single-element to multi-element (replace the existing `messages: [{ role: "user", content: userContent }]` line):
  ```typescript
  messages: [
    ...(conversationHistory ?? []).map((turn) => ({
      role: turn.role,
      content: turn.content,  // text-only for prior turns (D-08)
    })),
    { role: "user" as const, content: userContent },  // current turn with screenshot
  ],
  ```

  Do NOT change anything else — system prompt, userContent construction, token handling, SSE parsing, tier logic, and all callbacks remain identical. The change is additive and backward-compatible: callers that omit `conversationHistory` get exactly the same behavior as before (the spread of an empty array is a no-op).
  </action>

  <verify>grep -n "conversationHistory" /Users/subomi/Desktop/AI-Buddy/src/lib/ai.ts</verify>

  <acceptance_criteria>
  - `grep -n "conversationHistory" src/lib/ai.ts` returns at least 4 lines: interface field declaration, destructuring, spread in messages array, and the parameter comment
  - `grep -n "conversationHistory\?" src/lib/ai.ts` returns the interface field with `?` (optional)
  - `grep -n "role.*assistant" src/lib/ai.ts` returns a line inside the messages spread (prior turns include "assistant" role)
  - `grep -n "messages:" src/lib/ai.ts` returns a single line containing the spread operator `...`
  - TypeScript compiles without errors: `cd /Users/subomi/Desktop/AI-Buddy && npx tsc --noEmit` passes (run from project root)
  </acceptance_criteria>

  <done>
  `StreamGuidanceOptions` has `conversationHistory?: Array<{role: "user"|"assistant"; content: string}>`. The fetch body prepends prior turns before the current user message. No behavioral change when `conversationHistory` is absent or empty. TypeScript compiles clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → Worker | `conversationHistory` content (user text + Claude response text) is included in the outgoing POST body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | conversationHistory in messages array | accept | History is sourced exclusively from in-app signals (`sessionHistory` signal set by `onDone` callback from Claude API). No external input populates it. `guidance` text comes from Claude API responses already validated by the Worker. No user-controlled text injection path into history entries beyond what they already typed as intent. |
| T-09-01-02 | Denial of Service | Token bloat via large history | mitigate | D-09 caps history at 3 prior turns (enforced in Plan 03 `onDone`). This plan itself is stateless — cap enforcement lives in the caller. The `conversationHistory` parameter accepts whatever the caller passes; caller is responsible for the cap. Document this in the interface comment. |
</threat_model>

<verification>
Run from project root after completing the task:
1. `npx tsc --noEmit` — must exit 0, no type errors
2. `grep -n "conversationHistory" src/lib/ai.ts` — must return results showing interface field, destructuring, and messages spread
3. `grep -c "conversationHistory" src/lib/ai.ts` — must return 4 or more matches
</verification>

<success_criteria>
- `StreamGuidanceOptions` interface exported from `src/lib/ai.ts` includes `conversationHistory?: Array<{role: "user"|"assistant"; content: string}>`
- The Worker fetch body's `messages` array is built as `[...priorTurns, currentTurn]`
- TypeScript compiles clean (`npx tsc --noEmit` exits 0)
- Existing callers (SidebarShell.tsx) compile without changes (field is optional)
</success_criteria>

<output>
After completion, create `.planning/phases/09-state-machine-conversation-continuity/09-01-SUMMARY.md`
</output>
