---
phase: 14-code-signing
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/windows-beta-install.md
autonomous: true
requirements:
  - SIGN-05

must_haves:
  truths:
    - "docs/windows-beta-install.md exists in the repository and is ready to be linked from GitHub Release descriptions in Phase 16"
    - "The document explains why SmartScreen appears and gives step-by-step click-through instructions"
    - "The document includes screenshot placeholder markers for future screenshots"
    - "The document notes that EV code signing in v1.0 eliminates the warning"
  artifacts:
    - path: "docs/windows-beta-install.md"
      provides: "Windows SmartScreen click-through guide for beta users"
      contains: "More info"
  key_links:
    - from: "docs/windows-beta-install.md"
      to: "GitHub Release description"
      via: "linked in release notes / beta invite email"
      pattern: "windows-beta-install"
---

<objective>
Create `docs/windows-beta-install.md` — the Windows beta installation guide that documents SmartScreen click-through for users who download the unsigned Windows beta installer.

Purpose: Windows users downloading an unsigned binary see a SmartScreen warning. Without this guide, beta users will be stuck or assume the app is malware. This doc is what gets linked from the GitHub Release and included in the beta invite email.

Output:
- `docs/windows-beta-install.md` — complete SmartScreen guide with step-by-step, placeholder markers, and EV signing note
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-code-signing/14-CONTEXT.md
@.planning/phases/14-code-signing/14-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/windows-beta-install.md with SmartScreen click-through guide (per D-06, SIGN-05)</name>
  <files>docs/windows-beta-install.md</files>
  <read_first>
    - .planning/phases/14-code-signing/14-CONTEXT.md (D-06 — exact content requirements)
    - .planning/phases/14-code-signing/14-RESEARCH.md (Windows SmartScreen Documentation Spec section)
  </read_first>
  <action>
Create the `docs/` directory and write `docs/windows-beta-install.md` with the following content.

Per D-06, the file MUST include:
1. Why SmartScreen shows a warning (unsigned binary, expected behavior for beta)
2. Step-by-step with "More info" → "Run anyway" callouts
3. Screenshot placeholder markers using HTML comment format
4. A note that the warning goes away in v1.0 with EV code signing
5. A note to download only from the official GitHub Release URL

Write this exact content:

```markdown
# Installing AI Buddy on Windows (Beta)

> **Note:** AI Buddy is currently in closed beta. The Windows installer is not yet code-signed,
> which triggers a Microsoft SmartScreen warning during installation. This is expected and safe to
> bypass if you received the installer link from the AI Buddy team.

---

## Why Does SmartScreen Appear?

Microsoft SmartScreen warns users when an application does not have a code-signing certificate
from a known publisher. AI Buddy's Windows beta build is unsigned — this is intentional for the
closed beta period.

**This warning does NOT mean the app is harmful.** It means the binary has not yet gone through
EV code signing, which is planned for v1.0 (see note at the bottom of this page).

To protect yourself: only install AI Buddy from the **official GitHub Releases page**:
`https://github.com/[your-github-org]/ai-buddy/releases`

Do not install from any other source.

---

## How to Install (SmartScreen Click-Through)

### Step 1 — Download the installer

Download `AI.Buddy_x.x.x_x64-setup.exe` from the GitHub Releases page.

<!-- screenshot: step1-download-github-release.png -->

### Step 2 — Run the installer

Double-click the downloaded `.exe` file. SmartScreen will block it and show a blue dialog:

> **"Windows protected your PC"**
> Microsoft Defender SmartScreen prevented an unrecognized app from starting.

<!-- screenshot: smartscreen-blocked.png -->

### Step 3 — Click "More info"

Click the **"More info"** link in the SmartScreen dialog. The dialog expands to show the app name
and publisher details.

<!-- screenshot: smartscreen-more-info.png -->

### Step 4 — Click "Run anyway"

Click the **"Run anyway"** button that appears after clicking "More info".

<!-- screenshot: smartscreen-run-anyway.png -->

### Step 5 — Proceed through the installer

The installer will open normally. Follow the on-screen prompts to complete installation.

AI Buddy installs to `%LOCALAPPDATA%\AI Buddy\` and creates a Start Menu shortcut.

---

## After Installation

- AI Buddy starts in the system tray (bottom-right corner of your taskbar)
- Click the tray icon to open the overlay
- Use the push-to-talk shortcut to activate voice input

If you run into any issues, reach out to the beta support channel you received with your invite.

---

## Why Will This Warning Go Away?

In **v1.0**, AI Buddy will be signed with an Extended Validation (EV) code-signing certificate.
EV-signed applications are immediately recognized by SmartScreen without any warning — no click-through
required.

For the closed beta, bypassing SmartScreen via "More info → Run anyway" is the correct and safe
procedure.

---

## Frequently Asked Questions

**Q: Is it safe to click "Run anyway"?**
A: Yes, if you received this link from the AI Buddy team or downloaded from the official GitHub
Releases page. Only bypass SmartScreen for software you trust from a known source.

**Q: Why doesn't AI Buddy just sign the Windows binary now?**
A: EV code signing for Windows requires a hardware security key (HSM) for the private key, which is
incompatible with automated CI builds. We plan to implement this properly in v1.0 using Azure Trusted
Signing.

**Q: My antivirus flagged the installer. Is that normal?**
A: Antivirus products sometimes flag unsigned binaries from new publishers. If you downloaded from
the official GitHub Releases page, the file is safe. You can whitelist the installer in your
antivirus settings.
```

Note: Replace `[your-github-org]` with the actual GitHub organization name once the repository is public. This is the only placeholder that requires updating before the first release.
  </action>
  <verify>
    <automated>test -f /Users/subomi/Desktop/AI-Buddy/docs/windows-beta-install.md && grep -c "More info" /Users/subomi/Desktop/AI-Buddy/docs/windows-beta-install.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/windows-beta-install.md` succeeds (file exists)
    - `grep -c "More info" docs/windows-beta-install.md` outputs at least `2` (appears in step instructions and SmartScreen dialog text)
    - `grep -c "Run anyway" docs/windows-beta-install.md` outputs at least `2` (appears in step heading and body)
    - `grep -c "screenshot:" docs/windows-beta-install.md` outputs `4` (four screenshot placeholder comments)
    - `grep -c "EV" docs/windows-beta-install.md` outputs at least `1` (EV signing note present)
    - `grep -c "github.com" docs/windows-beta-install.md` outputs at least `1` (official download URL referenced)
    - `grep -c "SmartScreen" docs/windows-beta-install.md` outputs at least `3` (SmartScreen mentioned prominently)
  </acceptance_criteria>
  <done>docs/windows-beta-install.md exists with full SmartScreen click-through guide including why-the-warning explanation, 4 step-by-step instructions with screenshot placeholders, EV signing note, and official download URL</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Beta invite email → beta user | Users receive a download link; the doc must instruct them to verify it comes from the official GitHub Releases page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Spoofing | docs/windows-beta-install.md — download link guidance | mitigate | Document explicitly instructs users to download ONLY from the official GitHub Releases page URL; warns not to install from other sources |
| T-14-03-02 | Spoofing | SmartScreen bypass instruction | accept | Instructing users to bypass SmartScreen for unsigned beta software is standard practice; mitigated by source verification instruction (official GitHub URL) and the fact that it's a closed beta with known recipients |
</threat_model>

<verification>
```bash
# SIGN-05: File exists
test -f docs/windows-beta-install.md && echo "EXISTS"

# SIGN-05: Required content present
grep -c "More info" docs/windows-beta-install.md
# Expected: at least 2

grep -c "Run anyway" docs/windows-beta-install.md
# Expected: at least 2

grep -c "screenshot:" docs/windows-beta-install.md
# Expected: 4 (four screenshot placeholders)

grep "EV" docs/windows-beta-install.md
# Expected: line about EV code signing in v1.0
```
</verification>

<success_criteria>
- docs/windows-beta-install.md exists and is committed to the repository
- Document explains why SmartScreen appears (unsigned beta binary)
- Step-by-step click-through: "More info" then "Run anyway" with screenshot placeholders
- EV code signing note for v1.0 included
- Official GitHub Releases URL referenced for safe download verification
- No Windows-specific code signing performed (out of scope per DEFERRED)
</success_criteria>

<output>
After completion, create `.planning/phases/14-code-signing/14-03-SUMMARY.md` following the standard summary template.
</output>
